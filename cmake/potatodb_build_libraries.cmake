# -----------------------------------------------------------------------------
# Common files object library
# -----------------------------------------------------------------------------

add_library(potatodb-main OBJECT
  ${main_sources}
  )

add_library(potatodb-parser OBJECT
  ${parser_sources}
  )
