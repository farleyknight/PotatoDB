#pragma once

enum class CompareType {
  EQ  = 1, // equal
  NE  = 2, // not equal
  LT  = 3, // less than (<)
  LTE = 4, // less than or equals (<=)
  GT  = 5, // greater than (>)
  GTE = 6  // greater than or equals (>=)
};
